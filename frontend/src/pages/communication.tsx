import Comunicados from '../feature/communication/components/Communication';

export const CommunicationPage: React.FC = () => {
  return (
    <main className="page-wrapper">
      <Comunicados />
    </main>
  );
};